{
  "name": "my-azure-static-web-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build:frontend": "cd frontend && npm install && npm run build",
    "build:twilight": "cd twilight-zone && npm install && npm run build",
    "build:all": "npm run build:frontend && npm run build:twilight"
  }
}
